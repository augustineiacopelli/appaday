# appaday
The AppADay portal. A warm, editorial landing page listing every app in the archive — grouped by category, filterable by type, with live app count and streak stats. One file, no dependencies, no build step. Add a new entry to the APPS array to update the whole library.
